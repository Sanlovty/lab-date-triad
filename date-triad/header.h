#ifndef HEADER_H
#define HEADER_H
#include <tuple>
#include <string>

using std::string;
using std::to_string;
using std::get;
using std::tuple;
using std::isdigit;
using std::exception;
using std::stoul;
using std::abs;

using triple_t = tuple<size_t, size_t, size_t>;


#endif
